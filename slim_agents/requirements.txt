# SLIM Agent Communication Requirements

# Core dependency
# slim_bindings  # Install from ./slim/data-plane/python-bindings/

# Standard Python libraries used
asyncio  # Built-in, no install needed
sys      # Built-in, no install needed

# To install slim_bindings:
# cd ../slim/data-plane/python-bindings
# pip install -e .