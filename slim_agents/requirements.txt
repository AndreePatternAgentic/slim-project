# SLIM Agent Communication Requirements

# Main dependency - install with pip
slim-bindings

# Standard Python libraries (built-in, no install needed)
# asyncio
# sys

# To install:
# pip install slim-bindings

# You also need a SLIM server running on localhost:46357
# See README.md for SLIM server setup instructions